import { Text } from "@gluestack-ui/themed"
import { View } from "@gluestack-ui/themed"

type Props = {
  moduleId:number
}

export const ListLessons = ({moduleId}:Props) => {
  return(
    <View>
      <Text>Aulas</Text>
    </View>
  )
}